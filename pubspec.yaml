name: sistembrankas
description: "Smart Brankas Project"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.6.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 📷 Paket streaming kamera ESP32
  flutter_mjpeg: ^2.0.0

  # 🔥 Firebase dependencies
  firebase_core: ^3.6.0
  firebase_auth: ^5.7.0
  firebase_database: ^11.0.0
  cloud_firestore: ^5.4.0

  # 🔧 Tambahan umum
  cupertino_icons: ^1.0.8
  http: ^0.13.6
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
